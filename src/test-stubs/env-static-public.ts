// Stub for SvelteKit's `$env/static/public` virtual module in vitest.
// Real values are injected by SvelteKit at build time and are unavailable under vitest,
// so tests resolve this stub via the alias in vitest.config.ts.
export const PUBLIC_DEPLOY_ENVIRONMENT = '';
